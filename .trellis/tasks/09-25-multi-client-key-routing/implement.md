# Execution plan (after review/start)

- [x] Traced model-route auth, legacy/pipeline/cache/session-binding/management selection and diagnostic callers in `research/current-routing-contract.md`. Locked startup-only `PROXY_KEY` override, shared numeric cache target applied separately per owner, static model IDs versus scoped upstream catalog, and ID-less admin fallback; curated manifests validated.
- [x] `test/multi-client-keys.test.js` and existing suites use temporary DATA_DIR/local mocks for old config/env migration, malformed/duplicate inventory/owner byte preservation, full-list old-client saves, rotation/revoke/empty-key/CSRF and sanitized secret exclusions.
- [x] One bounded inventory and existing atomic config write owner hold legacy `proxyKey`, additional `clientKeys` and each account's stable `clientKeyId`; admin-only key API lists masked labels and reveals only generated create/rotate values while keeping `GET/POST /api/security` compatibility.
- [x] Request-local key ID scopes all chat/catalog account selection, same-key pre-stream replacement, six modes, role-aware pool/standby/growth/wait and owner-namespaced stateful binding; tests assert actual upstream Authorization under retries, races and empty/busy pools. Post-output SSE does not replay; admin explicit-ID probes remain independent.
- [x] Security/account UI retains full hidden account drafts and owner IDs, confirms destructive writes, clears one-time generated secrets on close/nav/401 and handles clipboard fallback/refresh failure. Local Chromium 145 at 390px tested Enter, focus/return, native confirmations, scroll and synthetic assign/rotate/revoke twice plus final focus-race regression (`research/browser-evidence.md`).
- [x] English backend/frontend specs, README and config.example synchronized. Final independent review and main-session gate: focused 278/278; two consecutive env-scrubbed full `npm test` runs 333/333 after one unexplained intermittent `parent-cross-feature` failure (isolated rerun passed; see `research/validation-report.md`). Node/server/lib/inline/research syntax, config JSON and `git diff --check` passed. Synthetic old-image rehearsal (run twice by research plus main rerun) proved direct v2 downgrade unsafe; full v1 backup restore is a future separately authorized deployment gate (`research/rollback-evidence.md`). Work commit/archival follow acceptance; no production access, live keys, paid upstream or deployment.

Rollback gate: old image **starts** on v2 but strips account owners and can route Legacy to a foreign-owned upstream. Fence writes, retain full v2 backup separately, restore the complete pre-upgrade DATA_DIR backup **before** booting the old image; never treat an in-place binary rollback as safe. Only synthetic copied-data evidence exists; production rollout requires separate authorization.
