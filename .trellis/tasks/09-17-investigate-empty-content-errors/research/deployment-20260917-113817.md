# 生产部署报告：详细日志转义误伤修复

## 发布身份

- Release：`20260917-113817-1ea9f29-detail-escape`
- Source commit：`1ea9f29d9fee0420948daa0e6d387c79e661e040`
- 修复 commit：`a056bbb`；任务记录 commit：`1ea9f29`
- Archive SHA-256：`6294733300d826652d4dd373ce677f5900f31ff0871023ba860e2529e9584390`
- Image ID：`sha256:ab68a343c7200929e792eee3d2bbb65f06927eca9d3843fa56ff9353ab4c1471`
- Previous release：`20260917-092017-cbdee55-cache-pool`
- Previous image ID：`sha256:b0833b3491f4c7dbfa2c7e83ed5ebdebc84d7f97280960e53cbd475a65e852d2`
- 安装：2026-09-17T11:39:28Z；切换完成：11:42:32Z；最终门禁：11:46:00Z。

## 发布源与变更边界

- 本地 deploy allowlist 与 committed HEAD 完全一致；工作区中的 `untitled.md`、`.pi/subagents/` 和另一个 Trellis 任务目录未进入 archive。
- 远端 archive 哈希及 12 个 release 文件逐一匹配 source manifest。
- 候选镜像中的 `server.js`、`public/index.html`、`lib/detailed-log-capture.js`、`lib/detailed-log-store.js`、`lib/jsonl-log-store.js` 与 committed release 哈希一致。
- 候选 Compose 与切换前 Compose 互相归一化验证，只有 image tag 与 build context 两处变化。
- 未修改 NewAPI、账号、Key、代理、Header、模型路由、cache-pool 策略、日志设置或历史日志。

## 数据与配置不变性

- `config.json` 部署前后 SHA-256：`a804534067b39e2f7056c66e41a1ed6c5978246fbfd399bc69808c7d3fbc87b4`。
- Compose 最终 SHA-256：`7d07ec266fcae93d0e9dbfca75baaf5ecb8e0fe0a8e4a2f4625de5c5136e8678`。
- `deployment.json` 最终 SHA-256：`872a17f3c28c8994a32b5c248e2c95553acf63e32ce961aa407ea26a1dc4487d`。
- 账号仍为 9 个、模式 `sticky`；当前 cache-pool 生产配置完整保留。
- `detailedLogging=true` 保持不变。

## 发布门禁

即时与 90 秒延迟门禁均通过：

- 容器 `running/healthy`，exact image ID 匹配，RestartCount=0，OOMKilled=false；
- 本地 `/api/meta`、认证 `/api/models`、`/api/accounts`、`/api/statistics`、request/detail log API 均为 200；
- quota refresh 非法空对象仍返回 400；
- statistics/API 账号数均为 9，账号模式仍为 `sticky`；
- detailed store `failures=0`、`corrupt=0`；
- NewAPI 容器内 `cline-pass-switcher` 内部别名可解析；
- 启动日志固定严重关键词均为 0；
- 公开域名在切换前后均因 DNS 不可解析而不可用，按项目契约记录为 `dns_unavailable_preexisting`，未替代任何内部硬门禁。

## 生产镜像合成验证

未发送业务请求，仅在运行镜像内直接调用已发布 `DetailRedactor`/`BodyCapture`：

- 完整普通 `\\uNNNN` / `\\xNN`：`state=complete`、正文非空且原文保持；
- 合成转义凭据：原始/解码秘密均不存在，安全 marker 存在，group 保持 complete。

历史已经发布为 0 字节的正文不迁移；修复仅作用于部署后的新详细日志。`resource-limited` 仍按原安全上限保持省略。

## 回滚与证据

- 远端证据目录：`/opt/cline-pass-switcher/verification/deploy-20260917-113817-1ea9f29-detail-escape/`
- 包含 source/install、build、切换、即时/延迟 API、启动日志、生产 witness、报告和 `backups/`。
- 旧 release/image、候选 release/image、备份、构建缓存和日志均保留，未清理。
- 回滚路径已冻结：恢复备份 config/compose/deployment 后以 `docker compose up -d --no-build` 拉回 previous exact image。
- 安全报告及 `deployment.json` 对生产已知敏感值内存扫描命中 0；未输出或持久化凭据到报告。
