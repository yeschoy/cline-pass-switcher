# Parent cross-child read-only review — current integrated tree

Scope: current `feat/pricing-console-keys-integration` at `d56e2f1`, independently tracing `server.js`, `public/index.html`, `test/` after reading parent PRD/design/plan and archived child implementation/results. No operator config/data/secrets, paid upstream, deployment, production load or business-code edits. Existing dirty parent plan/manifest/task files predated this review; do not overwrite them.

## Confirmed contract intersections (source + executable evidence)

- `server.js:917-944,971-977,2517-2552,2652-2671,2672-2711,2742-2781,4914-4919`: 12 v2 reference rows, v1 preserved, exact explicit input/output/cached-read only, known numeric zero vs missing, Qwen cached-write/context unpriced, retired V4 Flash not mapped to V4.1, final successful named/unknown Provider usage and immutable versioned amounts. Statistics do **not** carry client secret or owner ID; they are global administrator projections (`dispatch` admin guard at 4705-4740), while chat credentials only select an owned account. `test/integration.test.js:1288-1524` tests frozen v1/v2, retries, no usage, unknown Provider, zero and restart; `test/model-provider-ui.test.js` tests rendered versions and unknown/coverage. No per-key billing ledger is claimed.
- `public/index.html:160-183,1454-1485,1490-1582`: quota forecast remains an independent per-account $50 monthly-reference estimate, not offset by model consumption. The model view uses final request success samples; the channel view uses `(model, Provider)` attempt health, final-success usage and explicit unknown channel; detail shows price source, incomplete coverage and non-billing caveat. The accepted `/api/statistics` snapshot is local to the model/channel view; child switching/filtering does not refetch and unmounts inactive rows. `test/model-provider-ui.test.js` covers both views, stale reads, drafts and XSS escaping; archived UI `research/browser-results.md` records Chromium 390px keyboard/focus/scroll and dense 200×10 synthetic view (explicit channel view still ~200–224 ms task time). Note `/api/statistics` is separately refreshed in the general statistics view and in `loadAll()`: it is **not** one atomic snapshot across every section, so differing timestamps across independently refreshed sections are possible and not evidence of a valuation mismatch.
- `server.js:1420-1490,1997-2048,4376-4474,4648-4662,4694-4740,4914-4919,4974-5012,5096-5159,5215-5221`: client auth precedes model lease/network work; all lease modes and waits use the owner ID, stateful fingerprint is namespaced for non-Legacy keys; catalog uses an eligible owned account or returns no extra IDs. Admin Cookie/CSRF, not either client key, guards statistics/security/probes. Client key IDs are not attached to model usage/log projection. `test/multi-client-keys.test.js:148-316,342-447,588-812` asserts no foreign upstream attempt under empty pool, rotation, wait, cache, same session, retries and probes. Static known model IDs remain visible on authenticated catalog aliases even with empty pool; that is an intentional global public ID union, **not** foreign upstream catalog access.
- `server.js:148-189,1200-1250,2034-2047,4970-5011,5096-5159`: old accounts gain owner `legacy` with stable ID; old full account saves omitting owner retain it by explicit stable ID; malformed/foreign owner fails pre-write; empty Legacy plus extra keys rejects anonymous Legacy-owned accounts; original open single-key mode remains compatible. `test/multi-client-keys.test.js:148-275,820-880`, archived `research/rollback-evidence.md`, frontend `collectAccounts()`/owner draft tests support those statements. In-flight leased work is allowed to finish after rotation/reassignment, but new admission rechecks key/owner. Admin verifier remains independent (`server.js:2033-2077,4705-4740`, `test/admin-auth.test.js`).
- `server.js:225-237,1382-1394,2837-2938,3886-3905,4684-4694,4720-4732` seeds configured/effective/still-in-flight client secrets for bounded notes, ordinary reasons and sanitized detail. Request and error JSONL are bounded projections with no key/owner field; raw body capture is **separate, opt-in and cannot promise arbitrary secret redaction**. `test/multi-client-keys.test.js:447-493` checks synthetic secret absence from error response, metadata, ordinary log API and sanitized detail files. Administrator `GET/POST /api/security.proxyKey` deliberately remains a plaintext Legacy-only authorized compatibility exception.

## Evidence and residual acceptance gap

Executed on integrated branch with credential/base URL env overrides removed: `node --test test/parent-cross-feature.test.js test/multi-client-keys.test.js test/model-provider-ui.test.js test/admin-auth.test.js test/account-draft.test.js` exited 0; `node --test --test-reporter=tap test/integration.test.js test/ui-contract.test.js test/detailed-log-capture.test.js test/admin-ui.test.js` passed 177/177; `git diff --check` passed. Focused tests use isolated temporary DATA_DIR/local mock as written. No full `npm test`, new real-browser session, production or 300 RPM experiment was performed in this read-only review. `test/parent-cross-feature.test.js:46-168` combines quota/RPM/SSE/detail under Legacy anonymous and an unpriced `m`, **not** old→new key migration + dual-key priced models + v1 history + both UI tabs in a single fixture. This is a coverage gap, not proof of incorrect production behavior; recommend one copied/synthetic old-config integrated acceptance with distinct upstream Authorization and identical session, finalized v2 priced/unknown usage and frozen v1 cell, authenticated stats/UI projections and secret exclusion across ordinary/sanitized detail/metadata. Do not turn client isolation into an unsupported per-key statistics/billing promise.

Archived key child `research/validation-report.md` reports one unexplained intermittent `parent-cross-feature` detail lookup during its first full run, then isolated pass and two 333/333 full passes; root cause is unknown, so passing focused run here does not close a reproducibility investigation. If it recurs, record the status/body class (without secret/body contents) and determine whether the detail is 404/503/other before modifying code.
