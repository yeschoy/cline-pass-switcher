# Parent integration plan (planning only)

- [x] Confirm user intent, correct performance scope to whole-program review, inspect all critical flow owners; save static evidence in `research/perf-*.md` and two-run local synthetic smoke in `research/local-latency-results.md`.
- [x] Create four independently verifiable child tasks; parent does not start for business implementation.
- [x] Parent and child plans reviewed with the user. All four children received curated spec/research context, were started only after scope approval, independently checked, committed, archived and fast-forward merged into local `main`. Parent is now integration-only, not a new business-code owner.
- [x] **Whole-program performance child first:** `archive/2026-09/09-25-whole-service-performance/` is reviewed, committed and archived. Local synthetic source attribution identified per-chat metadata serialization/write as the dominant measured dense-history cost; compact runtime metadata retained per-terminal atomic writes. Representative routing/SSE, diagnostics/near-record-count logs, quota, direct/HTTP/SOCKS, management and real-browser 50/200-model × 0/3/10-Provider cases were recorded. A 2 ms statistics-read prototype was **not shipped** because benefit was indistinguishable from variance/risk. Handoffs: UI child retest dense Provider DOM scaling; key child review cold catalog's 12-way fetch stampede against key isolation. Real slow disk/TLS pressure, 100k detail roots, production-cadence quota and sustained throughput remain unverified; no 300 RPM production claim.
- [x] Price child `archive/2026-09/09-25-expand-reference-prices/` committed immutable v2 reference rates with DeepSeek ranges, historical v1 preservation, unknown/cached-write coverage, VM/integration/browser and isolated rollback evidence (`741db20`).
- [x] UI child `archive/2026-09/09-25-simplify-model-provider-console/` committed separate model/channel views, local snapshot navigation, bounded default DOM and Chromium 390px keyboard/focus/scroll and dense DOM checks (`19c952d`).
- [x] Multi-key child `archive/2026-09/09-25-multi-client-key-routing/` committed legacy migration, exclusive owner scoping across modes/cache/session bindings, admin key UI/secret redaction and browser/rollback research (`4250d88`); final synthetic 333/333 passed twice after one unexplained intermittent cross-feature test failure. **Direct old-binary rollback on v2 state is unsafe**. Integrated same-workload performance comparison remains a parent step.
- [x] Parent cross-child review: `test/pricing-key-integration.test.js` starts old ownerless config, creates new Key through independently authenticated admin, reassigns one account, exercises same Session/retry/two v2 priced models/sanitized detail and ordinary logs, then restarts and reasserts account scope/valuation; existing price-v1 and dual-tab VM/browser suites cover their distinct views/semantics. `research/integrated-performance-results.md` records two same-workload pre-key/current server pairs plus interleaved old/new UI A/B and limits. No raw-body promise or sustained 300 RPM claim.
- [x] Each child independently passed focused/full Node/VM/browser and spec/doc checks before its commit. Parent independent final check strengthened its isolated integration test/research assertions and passed env-scrubbed full `npm test` 334/334, runtime/inline/research syntax, task validation and `git diff --check`; parent work commit/archival follow. Deployment or target-host throughput requires separate authorization.

Rollback: each child is a separate work commit; **the old image accepts v2 but strips account ownership and can leak across client pools**. Fence writes, preserve full v2 state separately and restore a complete pre-upgrade DATA_DIR backup before starting old code in isolation. Only synthetic copies were rehearsed; never reset live credentials or delete operator data to roll back.
