# Execution plan — price reference and consumption equivalent

- [x] Resolve source: user wants current ClinePass table for the model/Provider page, DeepSeek tiers from its direct official page, and **no change** to the account monthly `$50 × remaining percent` forecast. Sources and alias caveat are in `research/price-source-drift-2026-09-25.md` and `research/deepseek-prices-2026-09-25.md`; existing quota formula/test were rechecked.
- [x] Integration + VM tests cover v1 preserved rate units/frozen cells, v2 exact 12 IDs/rates/sources/scale, DeepSeek interval, MiMo precision, Qwen unpriced, known zero vs missing/inconsistent usage, final Provider retry/unknown attribution, overflow and v1/v2 restart/malformed bytes. Independent check fixed misleading coverage labels and added price-count overflow.
- [x] Additive strict v2 validator and BigInt fixed-point pricing use existing `server.js` statistics owner, leaving v1 triple facts unchanged; no live price HTTP/billing or account quota mutation.
- [x] Existing model/Provider page now lists current tariff rows and supported consumption equivalents with source, DeepSeek range, partial coverage and separate quota wording; compact sub-tabs remain owned by the next UI child. Two real Chromium/390px keyboard/focus/scroll/DeepSeek/Qwen/monthly-quota smoke runs passed against the synthetic local fixture (`research/price-browser-evidence.md`).
- [x] English backend/frontend specs and README updated. Final independent review found no blocking issue: focused 158/158, env-scrubbed `npm test` 297/297, runtime/inline and research syntax checks, and `git diff --check` passed. Local synthetic v2→v1 copied-data rollback ran twice (plus one main-session rerun), with byte preservation and v1 restore (`research/rollback-evidence.md`); no production or paid upstream. Work commit and archive follow this accepted check.

Rollback gate: old price snapshot units, full old metadata/read projections and malformed-file byte preservation; older image compatibility of v2 metadata must be rehearsed before any separately authorized deployment.
