# Local multi-key acceptance and one intermittent gate failure (2026-09-25)

Scope: only repository test fixtures and isolated loopback/temporary `DATA_DIR`; no production data, paid upstream, deployment or push. The server/UI/security/spec changes were independently reviewed in backend, frontend and full-scope passes, with follow-up fixes for atomic config-before-runtime account saves, stable-ID-only owner inheritance, startup `CLINE_PASS_KEY` plus empty Legacy rejection, invalid/conflicting Header admission, generated-secret display before a fallible list read, and focus restoration after a dialog close/list redraw. The final browser sequence and old-image rehearsal are separately documented in `browser-evidence.md` and `rollback-evidence.md`.

Verification after the final focus and scoped-pool label changes:

- `node --test` for multi-key/auth/integration/low-pool/account-draft/admin-ui/ui-contract/detailed-capture/raw-detail: **278/278** passed.
- First `env -u CLINE_PASS_KEY -u PROXY_KEY -u PUBLIC_BASE_URL -u PORT npm test` after those changes had a single failure: `test/parent-cross-feature.test.js` at `group.attempts.length` (`TypeError: Cannot read properties of undefined (reading 'length')`). No foreign-owner upstream attempt was reported; this test combines detailed capture, RPM, SSE and quota refresh. Its isolated rerun passed. The error's cause was **not established**; source inspection notes that `DetailedLogStore.detail()` validates an `attempts` array before returning a group, so no speculative root cause or unrelated refactor was applied.
- Two subsequent full environment-scrubbed `npm test` runs both passed **333/333**; the separately focused `test/parent-cross-feature.test.js` passed **1/1**. These passing runs do not prove that the intermittent failure cannot recur.
- `node --check server.js`, each `lib/*.js`, the extracted production inline `<script>` via `vm.Script`, `rollback-check.mjs` syntax, Python browser checker AST, `config.example.json` parse, `git diff --check`, and task context validation all passed.
- `node research/rollback-check.mjs` after the final code still showed the old baseline **boots on v2**, drops account owners and lets Legacy reach a foreign-owned account; full pre-upgrade private-state restore (with separate v2 backup) passed. This is an explicit old-image production rollout/rollback blocker until separately authorized target-host preparation and verification.

Limits: not an exhaustive 24-order × every owner/race matrix; pre-`req.end()` provisional binding microstates were not directly paused in a black-box test, though owner-scoped HMAC binding namespace and concurrent same-session mock attempts were reviewed/tested. No production TLS/attestation, Docker image, Node 18 or screen-reader check occurred. Do not convert the synthetic 333-test result into a 300 RPM or production-safety guarantee. Test output under `/tmp/cps-*` is a non-business intermediate pending user-approved cleanup after task completion.
