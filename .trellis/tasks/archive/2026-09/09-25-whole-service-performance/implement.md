# Whole-flow execution plan (after planning review/start)

- [x] Load backend/frontend scenario specs and parent read-only audits/two-run smoke. Evidence and limitations recorded in `research/results.md`.
- [x] Build reproducible isolated **representative** harnesses across the whole service. `research/profile.mjs` covers startup, small/512 KiB/49 MiB chat, three attempts, 1/10 accounts and waiters, SSE/slow consumer/cancel, sparse/1200-bucket metadata, diagnostic modes, manual quota and management reads. `remaining-flows.md` adds 33k ordinary rows/2k detail roots, accelerated eight-account quota and serial direct/CONNECT/SOCKS reuse. `worst-cases.md` adds 49k/9k ordinary counts, 5k detail roots with async-read delay, 5 MiB sanitized capture and cold catalog stampede. `browser-results.md`/`browser-provider-density.md` measure 50/200 models and 0/3/10 synthetic Provider rows. Extreme 100k roots, actual slow disk/rename, TLS pressure, production-cadence quota and sustained throughput are explicit **unverified** boundaries, not claims.
- [x] Attribute 1200-bucket chat/GET slowdown with measured prune, sync stringify/write/rename and GET projection sections before editing; rank and defer in `research/results.md`.
- [x] Change only metadata encoding in existing atomic write owner, preserve per-terminal durability; add known-zero/restart/legacy-format regression test and use existing rename-fault suite.
- [x] Rerun the identical sparse/dense chat harness before/after compact metadata, plus two-run representative other-flow and actual Chromium/390px 200×10 Provider DOM scaling probes. Results, CPU/loop/RSS/heap and unmeasured environment limits are in `research/results.md` and linked reports; do not infer production or 300 RPM capacity.
- [x] Second pass on statistics GET: `research/statistics-read-profile.mjs` and `statistics-read-results.md` isolate model/Provider/health and serialization on sparse/dense v5 synthetic history; a single-pass prototype showed ~2 ms/GET p50 difference in one synthetic A/B pair, comparable to repeat-baseline variation, so it was reverted. Kept a focused historical price/known-zero/unknown/coverage/restart regression; no runtime projection change.
- [x] Independent review completed: focused integration + quota protection 120/120, env-scrubbed full test 295/295, syntax/inline VM/diff checks and `worst-cases.mjs all` passed. Startup-failure and SIGTERM cleanup were checked after hardening the browser fixture; the compact-metadata spec contract is synchronized. Main agent still owns the final commit (including Trellis artifacts); later children must repeat relevant cases.

Rollback: preserve prior metadata and tests; each measured optimization is a separate reversible change. Production deployment/performance validation is not authorized here.
