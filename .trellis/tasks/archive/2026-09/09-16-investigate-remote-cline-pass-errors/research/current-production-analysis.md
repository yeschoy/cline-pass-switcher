# 当前生产错误分析（只读）

## 结论摘要

- **检查时间**：2026-09-15 20:56:41–21:04:03 UTC；主聚合快照为 20:59:18 UTC。
- **生产版本**：`cline-pass-switcher:20260915-114348-diagnostics-quota`，镜像 ID `sha256:62963e5048f4ccdf83b8cdf88de28e517b39e44fdd53ea6b7b0d1a704e697ae0`。容器自 11:45:27 UTC 启动以来一直 `running/healthy`，重启 0 次，未 OOM。
- **基础设施不是当前根因**：主机负载低、约 55.8 GiB 内存可用、根分区使用率 45%；Switcher 标准输出只有 2 条启动期日志，未发现 fatal、未捕获异常、OOM、元数据持久化失败或日志写入失败标记。
- **界面的 1,187 条错误不是 1,187 个最终失败请求**：错误 JSONL 按失败尝试计数，覆盖 985 个请求 ID；请求 JSONL 按最终请求计数且保留起点更晚。当前可直接重叠关联的区间有 282 个最终失败请求和 282 条错误尝试；另有 905 条更早的错误尝试，其对应请求记录已不在当前请求 JSONL 中。
- **结合 2026-09-14 已有只读诊断，可证实完整错误保留区间内至少 885 个最终失败请求，最多再有证据缺口中的 100 个请求 ID 可能最终失败，因此当前证据给出的范围是 885–985，而不是 1,187。** 无法在不恢复已不保留的请求记录时给出更精确的全区间最终失败数。
- **当前容器版本自身产生的错误是 34 个真实最终失败请求**：363 个请求中 326 成功、34 失败、3 个客户端取消。34 条错误尝试与 34 个失败请求一一对应，不存在重复尝试放大。
- 当前版本的 34 个失败高度集中为两簇：
  1. 28 个非流式 `cline-pass/glm-5.3-flash` 请求在 14:41–14:42 UTC 返回 500，安全原因分类为 `empty_upstream_response`；
  2. 6 个流式 `cline-pass/deepseek-v4-flash` 请求在 16:14–16:23 UTC 于 HTTP 200 流已经开始后收到上游错误事件，其中最终状态 5×502、1×429。
- **旧版“流已完成却因下游关闭误报 502”没有在当前版本复现**：当前容器窗口、最近 24 小时及当前保留请求窗口中该签名均为 0；发布源同时包含 `[DONE]`/客户端取消判定修复标记和“客户端取消不写错误尝试”标记。
- 最后一个失败发生于 16:23:01.598 UTC。后续复核覆盖至 21:01:45.999 UTC；区间内 289 个请求中 286 成功、3 个客户端取消、0 个最终失败，说明当前不是持续性故障。

## 范围与安全边界

目标固定为：

- SSH：`ubuntu@167.114.158.4:49555`
- 远程根目录：`/opt/cline-pass-switcher`
- 容器：`cline-pass-console`

本次仅执行：本地路径/权限/gitignore 校验、只读 SSH、`docker inspect/ps/logs/image inspect`、只读文件哈希、远程进程内 JSONL 聚合、只读源码标记检查及 NewAPI 日志内存聚合。未调用任何写接口，未执行重启、部署、清理、配置修改、容器更新或日志修改；未读取详细日志正文文件。

SSH 身份文件只验证存在、权限和 gitignore：

- 文件存在：是
- 权限：`0600`
- gitignore：已命中
- 连接参数：`BatchMode=yes`、`ConnectTimeout=10`、严格主机密钥校验

所有账号均以稳定 SHA-256 前缀伪名表示；未输出账号 Key、代理 Key、Authorization、Cookie、代理 URL 凭据、请求正文或响应正文。

## 生产基线

### 主机

20:57:11 UTC：

| 指标 | 值 |
|---|---:|
| uptime | 3 周 1 天 12 小时 22 分钟 |
| load average | 0.54 / 0.23 / 0.18 |
| 内存总量 | 67,006,734,336 bytes |
| 可用内存 | 59,934,408,704 bytes（约 55.8 GiB） |
| 根分区 | 499,087,704,064 bytes，总使用率 45% |

没有资源耗尽证据。

### Switcher 容器与版本

| 字段 | 值 |
|---|---|
| container ID | `445a30f319d25d313062ca2b72c549fa3bac31cc4c3b99ae0364769ad5677144` |
| release/image | `20260915-114348-diagnostics-quota` |
| image ID | `sha256:62963e5048f4ccdf83b8cdf88de28e517b39e44fdd53ea6b7b0d1a704e697ae0` |
| image created | 2026-09-15 11:31:58 UTC |
| container created | 2026-09-15 11:45:27 UTC |
| started | 2026-09-15 11:45:27.389 UTC |
| status/health | running / healthy |
| restart / OOM | 0 / false |
| 数据挂载 | `/opt/cline-pass-switcher/data` → `/data`，读写挂载 |

发布目录与镜像内 `/app/server.js` 的 SHA-256 均为：

`584cd46668adb6d03563e8f733fa2f6f37fa8ddc23f32f8f0c04d0c88a82d74e`

该哈希也与本地 committed `HEAD` 的 `server.js` 相同；本地未提交工作树版本哈希不同，因此上述比较没有把未提交代码误认为生产代码。

只读源码标记检查：

- SSE `[DONE]`/客户端取消修复标记：存在
- `client_cancelled` 不写错误尝试标记：存在

## 日志完整性与覆盖范围

主聚合快照（20:59:18 UTC）：

| 日志 | 文件数 | 字节数 | 有效对象行 | 无效 JSON | 非对象行 | 不完整尾行 | 覆盖范围 |
|---|---:|---:|---:|---:|---:|---:|---|
| requests | 1 | 1,507,280 | 1,639 | 0 | 0 | 0 | 2026-09-14 13:44:10–2026-09-15 20:59:11 UTC |
| errors | 1 | 621,944 | 1,187 | 0 | 0 | 0 | 2026-09-13 12:42:44–2026-09-15 16:23:01 UTC |

请求与错误均无重复 identity。详细日志目录有正文类 `.txt` 文件，本次只看过文件元数据，未读取内容。

### 为什么 1,187 条错误不能直接等同最终失败

| 区间 | 错误尝试行 | 请求 ID | 当前请求日志可关联 | 结论 |
|---|---:|---:|---|---|
| 2026-09-13 12:42:44–09-14 06:16:59 | 801 | 603 | 否 | 与 09-14 历史报告完全吻合；历史报告已确认是 603 个最终失败请求 |
| 2026-09-14 06:17:00–13:44:10 | 104 | 100 | 否 | 证据缺口；96 个 ID 各 1 次尝试，4 个 ID 各 2 次，无法从当前文件判断最终成功/失败 |
| 2026-09-14 13:44:10–分析时 | 282 | 282 | 是 | 全部一一关联到 282 个最终失败请求 |
| **合计** | **1,187** | **985** | — | 885 个最终失败已确认；另 0–100 个可能最终失败，故证据范围 885–985 |

因此数量差异同时来自：

1. 错误日志统计“上游失败尝试”，请求日志统计“最终请求”；
2. 两类 JSONL 当前保留范围不同，早期错误仍在但对应请求记录已不在当前请求文件；
3. 历史错误按设计跨版本保留；
4. 当前版本新增错误只占 34 条。

## 三个时间窗口

### 全部当前保留请求（2026-09-14 13:44:10 起）

- 总请求：1,639
- 成功：1,351（200）
- 最终失败：282
  - 400：3
  - 429：141
  - 500：120
  - 502：18
- 客户端取消：6（499）
- 流式：957；非流式：682
- 错误尝试：1,187，但只有 282 条位于请求日志重叠范围；其余 905 条更早
- 旧版流完成后误报签名：0

### 最近 24 小时（2026-09-14 20:59:18–2026-09-15 20:59:18 UTC）

- 总请求：1,341
- 成功：1,268
- 最终失败：67
- 客户端取消：6
- 失败状态：52×500、11×502、3×400、1×429
- 失败模型：
  - `cline-pass/deepseek-v4-flash`：34
  - `cline-pass/deepseek-v4.1-flash`：5
  - `cline-pass/glm-5.3-flash`：28
- 小时分布（仅最终失败）：
  - 09-14 21时 4、22时 8、23时 4
  - 09-15 00时 4、01时 8、06时 1、07时 4、14时 28、16时 6
- 安全原因分类：50 个空上游响应、6 个中止传输、6 个流内错误事件、3 个其他 400、2 个其他上游 500
- 旧版流完成后误报签名：0

### 当前容器启动后（2026-09-15 11:45:27–20:59:18 UTC）

- 总请求：363
- 成功：326
- 最终失败：34
- 客户端取消：3
- 失败率：9.37%；但全部集中在 14:41–14:42 和 16:14–16:23 两段，16:23 后未持续
- 错误尝试：34，覆盖 34 个请求 ID，与 34 个最终失败一一对应
- 旧版流完成后误报签名：0

## 当前主要错误类别

### A. GLM 空上游响应突发 — 已确认现象；具体上游根因尚需验证

- 数量：28 个最终失败请求，28 条尝试
- 时间：14:41 UTC 11 个；14:42 UTC 17 个
- 模型：全部 `cline-pass/glm-5.3-flash`
- 请求类型：全部非流式
- 最终/尝试状态：全部 500 / 500
- 账号伪名：全部 `account#5739b59d2c`
- provider path：全部 `auto`
- attempt index：全部 0
- 账号动作：全部 `ignore`
- 安全原因标签：全部精确匹配 `empty_upstream_response`
- 代表请求：
  - `5439b840-510a-4fbf-9569-835b5c435613`，14:41:39 UTC，500
  - `6aae7cef-f1f1-4979-b578-7c5658a4355f`，14:42:31 UTC，500

证据解释：Switcher 确认收到无法形成有效内容的上游结果并最终返回 500，不是客户端取消，也不是主机/容器故障。下游 NewAPI 同期 14:40–14:44 的日志中有 30 条可解析为 channel error 500，其中 28 条不带额外 `failed` 标记，与 Switcher 的 28 个突发在数量和时间上接近；但 NewAPI 日志没有 Switcher 请求 ID，且同期有其他流量。该证据只能说明下游观察结果与 Switcher 失败时间一致，不能证明 NewAPI、某个 NewAPI 渠道或具体供应商是故障起因。

### B. 流开始后的上游错误事件 — 已确认现象；具体供应商原因尚需请求级下游证据

- 数量：6 个最终失败请求，6 条尝试
- 时间：16:14、16:16、16:17、16:19、16:20、16:23 UTC 各 1 个
- 模型：全部 `cline-pass/deepseek-v4-flash`
- 请求类型：全部流式
- 最终状态：5×502、1×429
- upstream HTTP 状态：全部 200，即流已经开始，随后 SSE 错误事件被识别
- 账号伪名：`account#d01f66b9e4` 5 个，`account#6231b53549` 1 个
- provider path：全部 `auto`
- attempt index：全部 0
- 账号动作：全部 `ignore`
- 安全原因标签：全部精确匹配 `stream_envelope_error`
- 代表请求：
  - `4a50f560-7233-4d94-ac51-70217f7d6a77`，16:14:33 UTC，502
  - `7f3fe09d-4c61-4540-8938-cfcb16ca7771`，16:23:01 UTC，502

证据解释：这 6 条不是旧版“客户端在 `[DONE]` 后关闭导致误报”的签名；当前记录明确是流内错误事件，且最终请求记录为 `failed`。下游 NewAPI 在 16:13–16:25 有 9 条 channel error 500，但没有请求 ID 可逐条对应；这只能作为时间一致性旁证，不能定位错误事件由 NewAPI、某个渠道、账号还是具体供应商产生。

### C. 客户端取消 — 已确认且与错误分离

- 当前容器：3 个；最近 24 小时：6 个；全部最终状态 499
- 错误日志中关联客户端取消的尝试：0

这符合当前版本预期，不能计入上游错误。

### D. 历史保留错误 — 已确认

当前 1,187 条错误尝试的安全分类：

| 类别 | 数量 | 主要时间分布 |
|---|---:|---|
| generic/other upstream | 619 | 其中 614 条位于 09-13 12–16时，历史报告已识别为通用/截断的 Cline/Vercel 流初始化失败；另 5 条较晚 |
| empty upstream response | 337 | 09-13 至 09-15，多段突发；当前版本占 28 |
| rate limit / quota | 149 | 09-14 12时 9、14时 140 |
| invalid input empty content | 60 | 09-13 16时 |
| aborted transport | 14 | 09-14 05时 1、20时 7、22时 6 |
| current stream envelope error | 6 | 09-15 16:14–16:23 |
| other upstream 5xx | 2 | 09-15 07时 |

脱敏代表证据：

- 历史 generic：`92b8e341-728e-4d25-aeaf-c5c5f584aeab`，09-13 12:50 UTC，502；其类别解释来自 09-14 历史报告，不重新输出原始原因。
- invalid input：`47ace850-a890-478c-af6d-f71e626d6fbd`，09-13 16:48 UTC，502。
- rate/quota：`bc27d4d1-97be-429c-812c-2c7fbb2a20a7`，09-14 12:17 UTC，429。
- aborted transport：`e3b0587e-d681-4269-9b20-863a4ed2069b`，09-14 22:06 UTC，502。
- 当前 empty response 与 stream envelope 代表请求见前两节。

这些历史数据解释了界面“大量错误”的主体，不能用来证明当前版本仍有相同故障。

## NewAPI 交叉验证

NewAPI 容器基线：

| 字段 | 值 |
|---|---|
| container ID | `7848b402ff888155fb711146906369c385c6526f1663791ed9720b13efc288b2` |
| image | `ghcr.io/yeschoy/new-api:079a527bc81bbd50d557861aed93a9949dd0524d` |
| started | 2026-09-15 12:23:34 UTC |
| status/health | running / healthy |
| restart / OOM | 0 / false |

近 24 小时读取最多 100,000 行的安全聚合实际得到 44,448 行。日志中没有 34 个当前 Switcher 错误请求 ID的直接文本匹配。每个 Switcher 错误时间 ±3 秒内都有 NewAPI 日志和可解析状态，但服务器同期承载其他渠道流量，时间重合不能替代请求 ID关联。

有用但非决定性的时间证据：

- GLM 突发窗 14:40–14:44：NewAPI 有 30 条 channel error 500，Switcher 有 28 个空响应 500。
- DeepSeek 窗 16:13–16:25：NewAPI 有 9 条 channel error 500，Switcher 有 6 个流内错误。
- Switcher 最后错误后至 21:02：NewAPI 整体仍有其他渠道的 400/503/channel error，说明不能把 NewAPI 全局错误直接归因于该 Switcher 通道；同期 Switcher 自身没有新的最终失败。

因此 NewAPI 证据等级为：**仅确认下游在相同时间窗观察到 500，与 Switcher 两段失败时间一致；不能据此判断故障起因，更不能逐请求确认具体渠道或供应商根因。**

## 根因排序

### 已确认

1. **统计口径与保留范围造成“大量错误”观感放大**：1,187 是尝试行，不是最终请求数；其中 905 条早于当前请求日志保留起点。
2. **当前版本真实失败只有 34 个且高度突发**，不是持续基础设施故障。
3. **28 个 GLM 非流式请求因空上游内容最终失败**；6 个 DeepSeek 流式请求因流开始后的错误事件最终失败。
4. **客户端取消已正确单列为 499，且不写错误尝试。**
5. **旧版 `[DONE]` 后关闭误报未在当前数据中出现**，签名为 0，生产源码也包含对应修复。

### 高概率

1. 14:41–14:42 的 GLM 失败是 Switcher 上游接口上的短时空内容异常，而不是主机资源耗尽或客户端取消；具体故障组件仍未定位。NewAPI 的同期 500 只作为下游时间旁证。
2. 16:14–16:23 的 DeepSeek 失败由 HTTP 200 后的 SSE 错误事件触发，而不是旧版 `[DONE]` 后关闭误报；错误事件由哪个上游组件产生仍未确认。

### 尚需验证

1. NewAPI 未记录/未输出 Switcher `requestId`，无法把 34 个请求逐一绑定到具体 NewAPI channel log、渠道 ID或供应商。
2. 09-14 06:17–13:44 的 104 条尝试覆盖 100 个请求 ID，但对应最终请求记录不再保留；这 100 个请求中多少最终失败无法由当前文件恢复。
3. 具体为什么 GLM 返回空内容、DeepSeek SSE 返回错误事件，需要 NewAPI 的请求级结构化追踪或上游供应商记录；当前安全日志聚合不足以区分账号额度、模型暂时故障和供应商实现问题。

## 最小处理建议与验证方式

1. **暂不重启、不清日志、不切换版本。** 当前服务健康，且 16:23:01.598–21:01:45.999 UTC 复核的 289 个后续请求没有最终失败；重启或清理只会损失诊断上下文。
2. **按两个短时间窗调查 NewAPI 对应渠道**：
   - GLM：14:41–14:42 UTC、模型 `cline-pass/glm-5.3-flash`、账号伪名 `account#5739b59d2c`；
   - DeepSeek：16:14–16:23 UTC、模型 `cline-pass/deepseek-v4-flash`、两个账号伪名。
   在安全环境内用代表请求 ID、时间和状态查 NewAPI 结构化记录；不要复制正文或凭据。
3. **增加或启用端到端请求 ID透传/结构化关联**（后续单独变更任务）：Switcher → NewAPI → 上游都记录同一个安全 request ID。验证标准是下一次错误可按 ID一对一关联，而不是仅靠 ±3 秒时间窗。
4. **界面继续明确两种计数单位并默认带时间范围**：分别显示“失败尝试数”“最终失败请求数”“客户端取消数”，标注日志保留起止时间。不要通过清历史日志掩盖统计差异。
5. **复发验证指标**：
   - 当前版本窗口 `old_stream_false_positive_signature` 持续为 0；
   - `empty_upstream_response` 和 `stream_envelope_error` 按模型/账号/小时监控；
   - 失败后至少观察 100 个后续请求及 NewAPI 对应通道状态；
   - 容器保持 healthy、restart=0、OOM=false。
6. 若必须得到全保留区间的精确最终失败数，只能从备份/归档恢复 09-14 06:17–13:44 的请求记录后离线关联；本次未授权也未执行恢复、迁移或清理。

## 检查前后不变性

检查前（20:57:11 UTC）与检查后（21:04:03 UTC）完全一致：

| 项目 | 检查前 | 检查后 |
|---|---|---|
| container ID | `445a30f...5677144` | 相同 |
| image ID | `sha256:62963e...697ae0` | 相同 |
| startedAt | `2026-09-15T11:45:27.389044065Z` | 相同 |
| restartCount | 0 | 0 |
| OOMKilled | false | false |
| health | healthy | healthy |
| `config.json` SHA-256 | `87d5ae2df08e1774ff76f35f186f27c12c8eb78469f415df3cf40403341d6089` | 相同 |

远程状态未因本次检查改变。
