# Quota exhaustion protection — implementation checklist

- [x] Trace error normalization, retry/rule ordering, every enabledAccounts/lease admission path, quota job owner/dedupe/backoff, persisted dispositions and `/api/accounts/recover` callers; use `research/existing-owner-review.md` and reconcile intervening commits. Preserve original response/status and no post-start replay.
- [x] Define strict persisted global two-decimal threshold ($0.01–$50.00, default $0.20) with older-client omission preservation, safe state normalization/migration and exact separate admin recovery UX/API. Test equal $0.20 boundary, invalid persisted bytes and account full-save round-trip before any mutation.
- [x] Implement real upstream HTTP 429 + explicit account quota + redacted bounded content signal → expiring provisional hold → same-owner forced quota refresh (even if routing quota pool off) → strictly post-signal fresh-monthly AND check → durable manual-release ban. Extend current owner/queue/timer, not a second job or fake page token; fail/unknown never persists a monthly ban.
- [x] Implement independent confirmed 5h/week temporary states with multi-window gating, fast local unavailability and bounded backoff. Reset time only schedules recheck; release requires successful new complete three-window proof with no monthly ban. Key/proxy edits fence stale pending/short-window work but retain a manual-release ban.
- [x] Test full AND truth table, default/custom/equality boundary, stale/failure/unknown and post-signal success, duplicate concurrent failures, one account vs another, 5h available+week exhausted, multiple reset times, month manual-ban priority, restart, manual release, key/proxy edits, pre/post SSE, original upstream status, ordinary log safety and pool-off operation. Use temporary DATA_DIR and local mock upstreams only.
- [x] Focus integration/UI tests, full gate and spec/operator docs. Local full gate 270/270 and isolated Chrome 390px synthetic-data keyboard/confirm smoke passed; see `check-report.md` for limits and metadata-write failure behavior. No live quota request or deployment was performed.
