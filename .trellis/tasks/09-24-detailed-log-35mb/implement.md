# Raw-body diagnostics — implementation checklist (planning)

- [x] Gate on completed independent admin auth and forced first password change. Ensure legacy client key cannot reach any detail settings/list/body API. Review backend logging, security and deployment specs before any code edit.
- [x] Trace all `DetailRoot`/`BodyCapture` construction, metadata projections and store readers/publish/expiry paths. Record measured local synthetic memory impact (not an exact RSS upper bound) for 35 MiB body x multiple captures before confirming 512 MiB reservation.
- [x] Add explicit default-off raw-body setting with strict persist-first validation; old detailed logging continues sanitized. Distinguish raw manifests/descriptors and preserve old group reads. Maintain protected Header/list projections and ordinary JSONL exclusions without a 35 MiB credential scan.
- [x] Enforce 35 MiB per body, 512 MiB retained budget, aggregate disk admission and 48h raw expiry in the **single store**. Place raw groups in its private child directory using an immutable timestamp+UUID path; delete only demonstrably owned expired groups even when manifest parsing fails. Preserve suspicious/unknown entries without serving or deleting them, and surface a safe health alert; keep legacy sanitized root and seven-day expiry. No body exposure through inventory, pagination, service logs or error responses. Retain fail-open chat lifecycle and request byte/status/lease parity.
- [x] Add authenticated on-demand raw body viewing with warning, no-store/nosniff, safe content rendering, browser lifetime/focus control; test unauthorized client key, logout, cross-origin and interrupted loads.
- [x] Local-temp/mock tests: fake secret in raw body readable only to admin, same fake secret absent ordinary/header/list projections; concurrent large bodies, backpressure, cleanup/restart/clear, 48h owned group expiry with corrupt manifest, suspicious file/symlink nondeletion/alert, shared disk capacity, legacy sanitized group and mode switches, SSE first-event/cancel parity. No real credentials or production data.
- [x] Update operator docs on explicit risk, 48h TTL vs external backups, admin-first rollout/rollback, and all changed English backend/frontend specs; run focused tests, Node checks, full env-scrubbed npm test and diff check. Do not deploy without separate authorization.
- [ ] **Production enablement gate outside this local implementation:** target-container concurrent 35 MiB requests/admin downloads, actual browser keyboard/focus/narrow-width acceptance, private backup-exclusion/48h expiry and old-image rollback isolation rehearsal; request separate deployment authorization.
