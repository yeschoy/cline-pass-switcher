# Execution plan — price reference and consumption equivalent

- [x] Resolve source: user wants current ClinePass table for the model/Provider page, DeepSeek tiers from its direct official page, and **no change** to the account monthly `$50 × remaining percent` forecast. Read-only sources recorded in `research/price-source-drift-2026-09-25.md` and `research/deepseek-prices-2026-09-25.md`; code/test/UI quota formula already match point 1.
- [ ] Add focused tests first for v1 price-version preservation, v2 exact 12-ID/rate/source/scale snapshot, DeepSeek Pro/V4.1 Flash interval, MiMo cached precision, Qwen Cached Write/context unpriced, unknown-vs-zero usage, inconsistent cache, final Provider attribution/retry, price overflow and v1/v2 metadata restart/malformed bytes.
- [ ] Implement an additive strict v2 validator and fixed-point calculator inside existing `server.js` statistics owner. Keep v1 triple arithmetic untouched, frozen per-request version cells and old supported price snapshots. No network price fetch on model requests, no invoice/remaining-balance mutation.
- [ ] Update existing model/Provider page's price facts and labels sufficiently to show all current per-model tariff rows and supported consumption equivalent with sources/DeepSeek range/coverage; coordinate compact tab presentation with the separate UI child. Leave monthly/5h/week quota calculations unchanged and keep the visual distinction clear.
- [ ] Update reusable English backend/frontend spec contracts and operator-facing README/example only where schema/API behavior changes. Run focused `node --test test/integration.test.js` and production UI VM/static suites, syntax checks for `server.js`/`lib/*.js`/inline script, credential-scrubbed `npm test`, `git diff --check`; verify local synthetic copied-data rollback/startup. Independent review and commit including Trellis artifacts. No production access or paid upstream.

Rollback gate: old price snapshot units, full old metadata/read projections and malformed-file byte preservation; older image compatibility of v2 metadata must be rehearsed before any separately authorized deployment.
