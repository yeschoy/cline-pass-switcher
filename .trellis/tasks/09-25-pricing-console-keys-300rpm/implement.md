# Parent integration plan (planning only)

- [x] Confirm user intent and inspect existing price, statistics UI, key auth/account routing and performance candidates; save evidence in `research/current-contract-and-hotpaths.md`.
- [x] Create four independently verifiable child tasks; parent does not start for business implementation.
- [ ] Review this parent plus each child PRD/design/implement with user. Confirm acceptance before starting any child. Curate real spec/research context in each child's `implement.jsonl` / `check.jsonl`; run `task.py validate`, then start **the target child**, not parent.
- [ ] Price child: verify source, write new frozen snapshot/pricing tests; retain old price versions and unknown/cached-write coverage. Run focused integration first, then relevant and full project checks.
- [ ] UI child: split tabs, simplify row text/details, preserve browser drafts/stale-response guards; run production VM/static tests and real-browser keyboard/focus/narrow-width checks. If pricing child not yet done, accept both old/new price versions and return after it for combined review.
- [ ] Multi-key child: use local-only generated fixtures; migration, strict config/API round trip, all selection modes (including cache pool/session binding/replacement), rotation/deletion/restart and auth/redaction regression tests. Review first `POST /api/accounts` / `POST /api/security` mutations and legacy env precedence, then full project checks.
- [ ] Performance child: check in a bounded local benchmark/test harness, baseline before optimization, measured hotspot fix with isolated correctness/failure tests, identical post-change run. Execute mixed workloads and record results/limitations rather than a universal performance claim.
- [ ] Parent cross-child review: staged load with old config; all existing accounts under legacy key, new key isolation with same session value, multi-model pricing and tab output, log/metadata/redaction exclusions, 300 RPM baseline after integrations. Verify independent admin login remains independent; no raw-body operational promise.
- [ ] Each changed code child runs narrowest relevant tests then `node --check server.js`, all `lib/*.js`, production inline-script `vm.Script`, `env -u CLINE_PASS_KEY -u PROXY_KEY -u PUBLIC_BASE_URL -u PORT npm test`, `git diff --check`; update English reusable specs and docs/examples for schema changes; independent checks and commit including Trellis artifacts. Deployment/production load requires separate approval.

Rollback: each child is a separate commit. For the persisted multi-key schema, prepare copied-`DATA_DIR` migration and old-image compatibility/rollback rehearsal before any authorized production rollout; never reset live credentials or delete operator data to roll back.
