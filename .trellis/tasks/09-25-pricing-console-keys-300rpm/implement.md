# Parent integration plan (planning only)

- [x] Confirm user intent, correct performance scope to whole-program review, inspect all critical flow owners; save static evidence in `research/perf-*.md` and two-run local synthetic smoke in `research/local-latency-results.md`.
- [x] Create four independently verifiable child tasks; parent does not start for business implementation.
- [ ] Review this parent plus each child PRD/design/implement with user. Confirm acceptance before starting any child. Curate real spec/research context in each child's `implement.jsonl` / `check.jsonl`; run `task.py validate`, then start **the target child**, not parent.
- [ ] **Whole-program performance child first:** profile startup, request, routing, transport, persistence, logs, quota, management and browser paths. Expand the local probe into a representative matrix; attribute the observed history-scale slowdown to specific CPU/I/O sections; rank frequency × cost. Optimize the highest proven low-risk bottlenecks in existing owners, with focused correctness/failure tests and identical post-change runs. Report unoptimized candidates honestly; do not force a 300 RPM pass.
- [ ] Price child: verify source, write new frozen snapshot/pricing tests; retain old price versions and unknown/cached-write coverage. Run focused integration first, then relevant and full project checks, including relevant performance probe.
- [ ] UI child: split tabs, simplify row text/details, preserve browser drafts/stale-response guards; run production VM/static tests and real-browser keyboard/focus/narrow-width/performance traces. Accept both old/new price versions.
- [ ] Multi-key child: use local-only generated fixtures; migration, strict config/API round trip, all selection modes (including cache pool/session binding/replacement), rotation/deletion/restart and auth/redaction regression tests. Recheck candidate selection/credential lookup hot paths against pre-change synthetic benchmark.
- [ ] Parent cross-child review: staged load with old config; all existing accounts under legacy key, new key isolation with same session value, multi-model pricing and tab output, log/metadata/redaction exclusions, rerun whole-flow baseline on integrated code. Include 300 RPM only as a separate throughput scenario, not the main performance verdict. Verify independent admin login remains independent; no raw-body operational promise.
- [ ] Each changed code child runs narrowest relevant tests then `node --check server.js`, all `lib/*.js`, production inline-script `vm.Script`, `env -u CLINE_PASS_KEY -u PROXY_KEY -u PUBLIC_BASE_URL -u PORT npm test`, `git diff --check`; update English reusable specs and docs/examples for schema changes; independent checks and commit including Trellis artifacts. Deployment/production load requires separate approval.

Rollback: each child is a separate commit. For the persisted multi-key schema, prepare copied-`DATA_DIR` migration and old-image compatibility/rollback rehearsal before any authorized production rollout; never reset live credentials or delete operator data to roll back.
