# Parent integration gates: migration, secrecy, rollback (read only)

This read-only inventory predates `test/pricing-key-integration.test.js`, which later connected old-owner migration, dual-key same-session upstream attempts, v2 usage, sanitized full-detail capture and restart in one synthetic fixture. Frozen v1 history and error-only detail remain covered by their separate child/diagnostic tests, not that new fixture. The old-image rollback gate remains unchanged.

- Auth/ownership: `server.js:152-189` validates canonical additional-key inventory and effective Legacy before migration; `server.js:1202-1220` normalizes existing accounts and rejects unknown owner before startup writes; injected `CLINE_PASS_KEY` is Legacy-owned (`server.js:175-184,1240-1256`). `server.js:4974-5012` validates full account candidates and persists config before live mutation; missing owner on an old save inherits **only by stable ID**, not name. New-owner selection is explicitly drafted in `public/index.html:671-810,1421-1432`. Client-only statistics/admin reads fail under the independent Cookie/CSRF guard at `server.js:4702-4740`; anonymous empty Legacy compatibility and empty owned-pool catalog/chat behavior are covered at `test/multi-client-keys.test.js:148-178,277-318`. No new confirmed cross-key routing defect found in inspected paths/tests.
- Sanitized-log boundary: `server.js:225-238,3886-3904,4684-4693` uses configured and in-flight client-secret seeds; `server.js:2837-2938` ordinary log rows contain account/model/provider/attempt facts, not key IDs or values; `.trellis/spec/backend/logging-guidelines.md` disallows raw sensitive content. Opt-in raw-body mode is **not** redaction-safe for arbitrary caller text (`public/index.html:194-200`, backend client-key spec). Browser extra-key list has ID/name only and one-time create/rotate secret (`public/index.html:707-767`); authorized legacy `/api/security.proxyKey` remains plaintext compatibility. Existing synthetic redaction tests use generated/synthetic keys only (`test/multi-client-keys.test.js:447-493`). Recheck separately if a new projection is added; this review did not inspect private operator data.
- **Blocking rollout/rollback condition, already known:** price child's archived `research/rollback-evidence.md` shows the pre-v2 price image rejects v2 metadata without byte loss; key child's archived `research/rollback-evidence.md` shows a different old binary can start on v2 config, strip owners and route Legacy to a foreign-owned upstream. They are different baselines and must not be conflated. On any authorized release: fence writes, privately back up the *entire* pre-upgrade DATA_DIR (including independent admin state and applicable logs), retain an isolated v2 backup, restore the complete old backup **before** starting old code, verify image/data hashes and isolate old admin/raw ingress. Direct old-binary-on-v2 rollback is unsafe. No target-host rehearsal or production authorization was present in this read-only review.
- Residual verification recommendation: run one clean synthetic copied-data end-to-end integration combining legacy v1 priced cells, old account ID/owner migration, authenticated two-client isolation under same session, error-only sanitized capture and current API/HTML projections after restart. Published independent child tests/Chromium checks are evidence for their individual owners, not a replacement for this cross-tree scenario. `test/parent-cross-feature.test.js` currently exercises a different Legacy-only quota/RPM/SSE/detail combination. Preserve the pre-existing dirty parent task files; no code/spec/task status/commit/deployment was modified here.
