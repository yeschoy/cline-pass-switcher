# Integrated-performance evidence boundary (read only)

This inventory predates the parent integration rerun. See `integrated-performance-results.md` for two same-workload server A/B pairs and interleaved old/new HTML browser A/B; its unmeasured sustained/production limits still apply.

Archived performance child `.trellis/tasks/archive/2026-09/09-25-whole-service-performance/research/results.md` attributes its **pre-key** dense-history local slowdown principally to synchronous complete metadata JSON serialization/write on completed chats: compact runtime metadata reduced 1,200-bucket local mock small-chat p50 from 19.82 to 14.35 ms, while the statistics GET remained ~17 ms. It covers startup, parsing/chat, account wait/SSE, quota, logs, catalog, management API, proxy transport and synthetic real-browser redraw; its statistics-read prototype was reverted because ~2 ms benefit was within run-to-run variation. The archived UI child browser review shows default 200 model rows vs explicitly selected 2,000 channel rows, with the latter still ~200–224 ms browser task time. Key child tests assert account-scoped RPM/lease/real attempts but provide **no same-workload integrated performance A/B** after price/UI/key merge.

`server.js:4648-4662` still issues separate cold catalog fetches (the performance child's local test observed 12 simultaneous cold reads → 12 local mock upstream requests), now scoped to client owner; this is a measured cold-only candidate, not evidence of per-key leakage or a universal production bottleneck. `server.js:4914-4919` aggregates per-model/provider historical cells on statistics reads; dense-history browser/API cost should be measured on the integrated tree, not inferred from the default DOM improvement. The new key inventory is bounded at 16 (`server.js:152-171`), and no per-key RPM limiter was added. The archived performance workload and later child workloads differ in config/schema/DOM size, so do not compare their absolute latency as a direct A/B.

Parent plan still calls for identical synthetic workload rerun after merging, including startup/cold-warm catalog, dense history, routing under two owners, retry/SSE, detail modes and console navigation; 300 RPM is an *optional total-service* throughput case, not a performance target or per-key guarantee. No integrated 300 RPM, sustained throughput, real slow disk/TLS pressure, 100k detailed roots or production-cadence quota measurement was performed in this read-only pass; no production SLO or rollback/deployment safety claim follows from the available evidence. Recommend performing isolated repeatable benchmark/real-browser acceptance separately, with temporary DATA_DIR/local upstream, measuring median/tail, CPU/event loop/RSS/I/O and queue-drop/fairness before any further optimization. Do not bypass configured per-account maxRpm/maxConcurrent for a synthetic throughput target.
