# Parent Phase 2.2 cross-task acceptance check

## Result and scope

2026-09-23, main `ecd3ea2`, Node v26.8.1. **Local-mock/code-gate PASS for AC1–AC8 within the documented protocol scope.** No production data, credentials, paid upstream, New API source, deployment, push, commit or archive was touched. The parent PRD/design/task.json changes already in the worktree belong to the main session and were preserved. This check added one independent cross-feature test (`test/parent-cross-feature.test.js`), corrected two stale documentation statements, and did not change product runtime code.

| AC | Evidence and residual |
|---|---|
| AC1 | `record()` emits bounded ordinary projections and optional `(attemptIndex, detailCallId)`; `clineRequest()` assigns the native token after `req.end()`. `test/integration.test.js` `error-only detail correlates...`, full-detail tests, `test/detailed-log-ui.test.js` exact double-token test, and new cross-feature test confirm off/error/full, failure-only groups, safe on-demand body, retry/replacement indexing and ordinary credential absence. Transport no-response/post-start-break and missing-group UI states have focused coverage. No claim that a missing group has a per-request durable failure reason. |
| AC2 | `tryLeaseResult()` checks concurrency before reserving first RPM permit; `clineRequest()` commits at `req.end()`, retries call `lease.takeRpmPermit()`. RPM suite in `test/integration.test.js` covers saturation, pre-send release/post-send retention, local 429, management callers, cancellation, restart and mixed/no-grow. New test sees low 2 + high 1 real commits over three attempts and no extra ping commit. `selectionBlockFacts()` **does** read/prune RPM for a truthful mixed-block diagnostic after a failed concurrency selection; it never reserves/consumes RPM there. README wording was corrected. |
| AC3 | `normalizeAccount()`, `/api/accounts` strict save/old-client preservation, `collectAccounts()`/drawer and VM tests retain `maxRpm`; config example and backend/frontend specs carry the canonical integer. No real browser was run. |
| AC4 | `cachePoolMembership()` preserves legacy low=0 ordering and derives target/actual on low>0. `buildPipelineGroups()` applies low→high→unknown before health/sticky. `test/low-quota-pool.test.js` covers 80/95 boundaries, known zero, unknown filler, RPM/concurrency high fallback, binding and high-only growth; integration tests cover low=0/grow-only target. New test sees held low removed and actual `{high:1,low:0,unknown:0}`. |
| AC5 | `settleAttempt()`/`persistLowQuotaHold()` establish independent waiting-refresh and removal; `reconcileQuotaDisposition()` keeps partial non-100 unknown held, confirms partial known-100 exhausted, and clears only on later real complete success. `test/low-quota-pool.test.js` covers rule/quota orthogonality, resets, failure, restart, one pre-output replacement and post-start/cancel boundaries. New test blocks the quota HTTP response until hold, detail and SSE facts are visible, then releases a real refresh and observes low re-entry. |
| AC6 | `JsonlLogGroup` single-row 64 KiB and pending 10,000/16 MiB fences plus fail-open tests; `BodyCapture` 5 MiB / global 64 MiB, error-only success 50 MiB no-copy test; `record()` owns one terminal `saveMeta()` (instrumented integration test), deadline-bound shutdown/drain tests cover complete response, active SSE and blocked writer. Same-machine samples below. CPU sanitizer is **bounded/deferred/fail-open on the main JS thread**, not nonblocking. |
| AC7 | Archived New API source analysis `research/newapi-keepalive-compatibility.md` and `09-21-newapi-chat-keepalive/check-report.md` establish HTTP/1.1 pooling, scanner comment filtering and protocol limits; current `server.js` direct/proxy Agents, inbound idle, first-data wall timer, stream-idle reset and stream-local `Writable` are exercised by local integration tests including `write(false)→drain`, prelude, quiet ping, idle error and SIGTERM. New test joins quota/RPM/error detail with the quiet SSE path. No real New API or Docker E2E was run in this round. |
| AC8 | Focused suites and full Node 26 gate below passed. README, example JSON and reusable backend/frontend/cross-layer specs were traced; one outdated Node-18-verification sentence in `backend/database-guidelines.md` was corrected with the archived report as evidence. Real-browser layout/focus/keyboard/clipboard/narrow viewport remains unverified because `agent_browser` has no binary; VM/static UI tests do not substitute. |

## Cross-feature scenario and mutation discrimination

The new local mock uses a temporary `DATA_DIR` and a successful three-window low/high seed. One SSE request sends low/provider-first HTTP 500, low/provider-second HTTP 401 account degradation, and high/provider-first HTTP 200. It asserts three actual `(account, provider)` paths, 2/1 committed RPM, one terminal success/usage/request row, two ordinary failure rows with original upstream statuses, two exact detail `(requestId, attemptIndex, callId)` matches and sanitized on-demand bodies, no success body copied, low `waiting-refresh`, high fallback, real refresh clearing the hold, restored low routing, a quiet SSE `: PING`/`[DONE]`, and post-SIGTERM group/row survival. It does **not** claim to independently exercise HTTP proxy reuse, idle upstream timeout, client cancellation or writer failure; existing focused tests own those.

Isolated copy mutation at `/tmp/cps-parent-mutations.om61hn` (product tree unchanged): forcing `SSE_HEARTBEAT_MS=0` failed the new test's ping assertion (exit 1); forcing `buildPipelineGroups()` `roleAware=false` failed the first account/provider path assertion (exit 1). A deliberately narrower mutation disabling only role-aware **membership** survived this two-account/target-two test (exit 0): the scenario tests role priority and removal, not a shortage-driven membership change. That separate owner is tested by `test/low-quota-pool.test.js`; the surviving mutation is a known detection limit, not a PASS claim. Mutation logs and synthetic ordinary benchmark data remain in the temporary directory pending operator consent to clean up.

## Verification commands and counts

- `node --check server.js`; `for file in lib/*.js; do node --check "$file"; done`; inline production HTML `vm.Script` compilation; `node --check test/parent-cross-feature.test.js`: all pass.
- `node --test test/jsonl-log-store.test.js`: 8/8.
- `node --test test/detailed-log-capture.test.js test/detailed-log-store.test.js test/detailed-log-ui.test.js`: 70/70.
- `node --test test/ui-contract.test.js test/account-draft.test.js`: 40/40.
- `node --test test/low-quota-pool.test.js`: 22/22.
- `node --test test/parent-cross-feature.test.js`: 1/1.
- `node --test test/integration.test.js`: 101/101.
- `env -u CLINE_PASS_KEY -u PROXY_KEY -u PUBLIC_BASE_URL -u PORT npm test`: **242/242 pass, 0 fail** after the final test edit.
- `git diff --check`: exit 0. For the two untracked files, `git diff --no-index --check /dev/null <file>` reports no whitespace diagnostics but exits 1 because additions differ from `/dev/null`; a separate whitespace/newline assertion passes.

This round did **not** re-run Node 18. The archived child report documents a SHA256-verified official Node 18.20.8 darwin-arm64 full **241/241** pass before this new test; that downloaded binary was removed with user consent in the earlier session and is not represented as a current artifact.

## Resource and performance facts (not portable thresholds)

- Current Node 26 in-memory single-body production `BodyCapture` + `DetailRedactor` learn/materialize on a ~5 MiB valid JSON diagnostic: two independent samples (the same capture boundary used by both profiles) **85.52/76.79 ms wall**, 5,242,661 input bytes → 5,242,681 sanitized bytes; heap delta +15.13/−15.34 MiB (GC/noise), event-loop monitor p99 11.03/91.88 ms; retained reservation 10,485,362 bytes and **0 after release**. The first p99 does not include a representative delayed tick; these are neither end-to-end profile timings nor evidence of nonblocking CPU. The earlier archived child performance report separately records a local mock successful 50 MiB error-only ingress with zero retained capture and an approximately 118 ms 5 MiB event-loop p99; that measurement was **not** repeated here.
- Current same-machine 5,000 projected ordinary rows (async append all admitted/settled, temporary directory): **70.95 ms wall**, event-loop p99 **14.12 ms**, heap delta **+4.06 MiB**, pendingRecords/pendingBytes **0/0** at completion, dropped/failures **0/0**. Fixed queue fences and blocked-writer fault behavior are established by focused tests, not this throughput number.
- `test/integration.test.js` observes direct HTTP/HTTPS, HTTP CONNECT and SOCKS tunnel reuse, inbound HTTP/1.1 reuse across >5 seconds, SSE scanner/idle/backpressure and one metadata atomic write at successful finalization. The new scenario records one local mock socket observation per real chat attempt but does not independently prove reuse; no actual New API connection count was measured.

## Remaining verification boundary

A staging topology check against a real New API instance (configured with its own downstream ping if needed), Docker runtime, and a real browser keyboard/focus/responsive/ARIA/clipboard inspection are still required before claiming those operational properties. New API ordinary Chat cancellation before Switcher response headers is an external known limitation: the Switcher first-event deadline bounds residual work but cannot observe the original client instantly. No production switch enablement or deployment was authorized.
