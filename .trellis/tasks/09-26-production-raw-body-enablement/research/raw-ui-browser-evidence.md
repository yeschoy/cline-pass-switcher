# Local raw-detail browser check (synthetic, 2026-09-26)

Command: `python3 .trellis/tasks/09-26-production-raw-body-enablement/research/raw-ui-browser-check.py` — **passed** in local headless Chromium (390 × 844 CSS px). The script serves the actual `public/index.html` on a temporary loopback `ThreadingHTTPServer` and supplies only synthetic authenticated JSON/body responses; it makes no remote or paid upstream request. The first Playwright attempt used `locator.check()` for a deliberately cancelled checkbox mutation, which correctly raised because the UI reverted it; the fixture was corrected to `click()` and the complete browser check then passed.

Observed: cancelling the native risk confirmation sent zero POSTs and restored unchecked state; accepting sent one CSRF-bearing POST and reflected checked state. The selected `raw-error` detail rendered only safe `Authorization`/`Set-Cookie` names with `[REDACTED]`; no body was fetched before explicit selection. Keyboard Enter on the body button loaded its synthetic text; metadata received focus and did not overflow horizontally at 390px. Navigation cleared the loaded text/copy control; a simulated 401 on a later body read opened login and cleared it again.

This checks visible Chromium behavior for the fixture, **not** production login, a real service's Header/store validation, clipboard permissions, screen readers, all viewport sizes, cgroup load, old-image rollback, backup expiry or any live raw content. No screenshot or credential was saved. `agent_browser` was attempted first but unavailable because its `agent-browser` binary is not on PATH; installed Python Playwright/Chromium was used as a local-only fallback.
