# Production read-only / synthetic isolation preflight — 2026-09-26

**Authorized boundary:** operator approved read-only production checks and isolated/private-copy rehearsal, **not** a release, live Compose/config write, memory/flag change or raw enablement. The current source candidate is committed only on `feat/raw-detail-safe-headers` (`a8fb4da`; task record `90c96cf`); local `main` equals `origin/main` at `9aaa691`, so the normal committed-`main` release source is **not ready**. Local Docker CLI is present but its daemon is unavailable; the remote daemon is available. Do not call a feature-branch rehearsal image the exact normal-release candidate.

## Verified read-only facts

- Repository-local SSH identity was checked for mode `0600` and gitignore status without reading it. Bounded batch SSH worked; remote `python3`, `sudo -n` and Docker were available. No source archive was uploaded.
- Live `cline-pass-console` remained `running/healthy`, zero restarts, OOM false on image `sha256:0bd1deaefb04b6c2c3f1e3417eddc07b3fc6dd6398212ff98e6a783c3c62100c`, memory limit **536,870,912 bytes**, no raw-ready environment entry. Safe config projection: 10 accounts, sticky mode, both detail switches true, raw=false, no additional client-key inventory. Free disk was approximately 420.9 GB; host memory was ~67.0 GB total/~61.0 GB available, which does **not** raise the container cgroup limit.
- Live config SHA-256 `e985edba49cf50aa25974db25accc53a9681e8964fcb05348854b9e889820731` remained different from `deployment.json.configSha256=9f98da7029d6adf82ea74ddd53a984661319fc61f9e3f43bdf32f3693432fe16`. The record still says `awaiting-admin-acceptance` on `32a150e`; no record/config mutation. A bounded private scan found **zero** matching recorded-hash files among two `backups/` and 43 `verification/` files named `config.json`, and zero matches among 232 readable bounded `.json` files. The identified pre-release backup config hash also differs; a top-level-only safe comparison found the low-quota-slot field, normalization fields and **ten other top-level differences** that were intentionally not printed. Stable account-ID sequence matched. This is **not** adequate evidence to accept the older release or infer all drift is the intended slot save; preserve current bytes and seek an authoritative rehearsal snapshot plus operator intent separately.
- Existing `data/` and `data/detailed-logs/raw/` were mode `0700`; `verification/` was `0700`, but the enclosing `backups/` directory was `0775` (the specific release backup directory was `0700`). Two backup config/metadata copies and 34+ verification copies were present; no `raw/` child appeared in these bounded historical copies. This does **not** prove that future ordinary backups exclude raw groups. Root/ubuntu crontab queries returned no usable listing, host scheduler config files contained no Switcher root/raw-exclusion path, and two unrelated backup timers existed. External, ad-hoc or off-host copiers remain unproven. **Backup exclusion/independent private 48h expiry gate is blocked.**
- The recorded previous image `sha256:2747552c4fed2b9cbee9461682a6b5cf50fba47b77a209f55c7e028982047571` was present. Inside a disposable hardened Docker container (`--network none`, read-only root, UID/GID 1000:1000, dropped capabilities, no-new-privileges, tmpfs), a **synthetic** raw-group manifest/body in private tmpfs was rejected by the old validator; its list did not include the raw root and detail/body reads were denied. This proves only old-image *module* behavior on that synthetic fixture and container network isolation. It does **not** rehearse the live gateway/management ingress isolation or the complete v1/v2 config+metadata rollback. No real raw body/credential was read or copied.

## Stop / next gates

1. Do not mark resource-load or deployment source accepted: the exact new committed-`main` Compose-built image does not exist, and local Docker cannot run the target cgroup. Merge/push the reviewed feature to `main` **only with separate authorization**, then build a private isolated candidate from the committed source, not from dirty files or an emergency feature-branch release. Keep live raw=false.
2. Verify *every* copier's future raw-directory exclusion or a hard private 48h expiry, and rehearse the actual backup/restore and old management ingress fence. Historical absence of raw groups while raw=false cannot prove exclusion.
3. Keep the September 24 deployment record pending until an authoritative private snapshot and operator testimony resolve the current safe changed-field evidence. A negative or unauthenticated API probe cannot substitute for administrator acceptance.
4. Only after these source/backup gates, continue exact-image >=2 GiB concurrent load and private migration/recovery rehearsals; later present a measured live cutover for separate approval. Post-check Compose/config/deployment hashes and running image/memory/health/restart/OOM facts matched preflight; `metadata.json` hash changed while the service was live (`83689577…` to `b6e2e31c…`), so no metadata byte-stability claim is made or attributed to this read-only inspection. No cgroup, Compose, config, image tag, public tunnel, gateway or production service was deliberately changed in this preflight.
