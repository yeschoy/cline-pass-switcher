# Raw-detail candidate and production admission — execution plan

**The operator approved the reviewed plan for local code-candidate implementation only.** The live cutover is a separate approval gate; choosing committed `main` as a planned source is not authorization to deploy it or change production. If any gate fails, retain raw=false and report the blocker.

## A. Reviewed local candidate (approved for local implementation)

- [x] Freeze the current branch/source and task scope; read backend logging/auth/persistence/deployment, frontend state/quality and cross-layer guides. Trace the three settings, route/profile selection, `DetailRoot`, Header sources, raw manifest validator, authenticated detail APIs, selected-content UI and their tests. Preserve unrelated working-tree changes. On a feature branch based on current `main`, implement **only** the safe raw Header/detail projection and UI warning needed by the confirmed contract; leave pricing/multi-key code unchanged.
- [x] Preserve default-off independent raw opt-in, >=2 GiB/ready gate, full-over-error single group, existing sanitized modes, raw body/retention/budget/reader limits, strict manifest validation, fail-open model traffic and persist-first settings API. Raw full: ingress/each upstream attempt/downstream safe request/response Headers plus existing raw bodies. Raw error: failed-attempt request/response safe Headers plus raw bodies, including failed-then-successful request; no successful-attempt content or duplicate group. Specify a bounded name/known-safe structural-value allowlist; all sensitive or unknown Header values become redacted/omitted, never `Authorization`/Cookie/Set-Cookie/key values. Raw list/ordinary JSONL/metadata remain Header/body-free. Handle old raw groups with absent Header maps and legacy sanitized groups without migration.
- [x] Update the existing `DetailRoot`/`DetailedLogStore` schema and `public/index.html` selected-detail rendering/wording; make raw-enable require an explicit one-time-per-enable risk confirmation without auto-posting on load, and preserve toggle failure/stale-generation/focus/navigation cleanup. Update focused capture/store/raw/integration/admin-auth/UI/browser-contract tests, operator docs and reusable specs **only after** verifying the final executable contract. Test malicious Header name/value, custom Header, Cookie/Set-Cookie, URL-credential, overlong/duplicate/array values, invalid raw manifest, old record, 401/CSRF, persistence failure, failed-then-successful attempt, SSE/cancel, and raw-off parity.
- [x] Run focused capture/store/raw/Header/admin/UI tests, `node --check server.js`, `for file in lib/*.js; do node --check "$file"; done`, production inline-script `vm.Script`, `env -u CLINE_PASS_KEY -u PROXY_KEY -u PUBLIC_BASE_URL -u PORT npm test`, `git diff --check`, and an independent cross-layer/security review. The local browser fixture also passed at 390px; see `research/local-candidate-validation.md`. A local suite is not cgroup or live proof.
- [x] Commit the locally checked source candidate as `a8fb4da` on this feature branch; task records are committed separately. Merge/push to `main` only after a later release approval; build only committed `HEAD`. This source commit does not complete the production gates below.

## B. Isolated release and safety gates (raw remains false live)

Read-only preflight and one synthetic previous-image raw-read denial were performed; see `research/isolated-preflight-20260926.md`. **None of the B checkboxes is complete:** the candidate is not on committed `main`, future backup exclusion is unproven, the older config drift remains unresolved, and the exact-image cgroup/migration/gateway rehearsals have not run. Do not infer approval to merge/push or mutate production from this stage.

- [ ] Read-only freeze actual production image/health/restarts/OOM, memory/flag, config/metadata hashes, Compose/env/gateway, admin/deployment record and backup inventory without printing operator data or secret values. Resolve older September 24 release acceptance **separately**: attribute actual administrator protected-page testimony; find an authoritative private pre-save/rehearsal snapshot and compare only safe changed field paths. If unmatched, keep `awaiting-admin-acceptance`; no silent `deployment.json` completion or config overwrite.
- [ ] On restrictive private copies, rehearse committed candidate's pricing/multi-key migration (all existing accounts assigned to Legacy, stable IDs, scoped routing, old v1-vs-v2 metadata compatibility), hash predicted allowed changes and preserve complete pre-upgrade v1 config/metadata/admin/Compose/gateway plus post-upgrade v2 state. Prove exact old-image rollback **cannot** run on v2 state: fence writes, isolate old management ingress and raw child, restore compatible full v1 state only behind an unchanged-hash guard; prove pre-mutation failure is a no-op. If operator saves drift, preserve both versions and stop. Never transplant a production raw body into research fixtures.
- [ ] Inspect all root-private release/ordinary/scheduled/external DATA_DIR backup paths. Prove `data/detailed-logs/raw/` exclusion on the **actual** backup/recovery combination, or separately enforce private 48h expiry and test private restore; otherwise stop. On synthetic copied state prove 48h expiry/recovery (including owned corrupt manifest), older-image raw manifest/body denial, symlink denial and management ingress isolation. Preserve exact old image, private backups and rollback helper rehearsal; do not prune them.
- [ ] Run the exact candidate image under proposed >=2 GiB cgroup and production hardening against isolated synthetic DATA_DIR/local mock upstream. Enable raw **only in the isolated fixture**; combine concurrent near-35 MiB HTTP/SSE, failures and retries, slow authenticated downloads, normal chats, cancellation and restart. Measure RSS/heap/cgroup peak, event-loop delay, CPU, OOM/restarts, drops, TTL, chat bytes/status/leases/statistics/ordinary logs and safe Header projection. Review measured headroom and select a concrete target limit; do not weaken the 512 MiB reservation/64 MiB subset fence or assume 2 GiB is sufficient. Stop on privacy leak, degraded chat, OOM or unacceptable margin.
- [ ] In an actual browser against synthetic fixture, verify independent admin login, one-time enable warning/explicit choice, all three settings, safe request/response Header display, on-demand body, clearing on navigation/logout/session loss, keyboard/focus/narrow width and client-key denial; verify Origin/CSRF and `no-store`/`nosniff`. Never screenshot/export live raw content or credentials. VM/static tests alone are not browser proof. Re-run complete checks against **the committed source used to build the candidate** and save only bounded non-secret evidence.
- [ ] Present a live-cutover sheet: exact candidate commit/image, measured cgroup limit/headroom, migration/hashes/account ownership, backup exclusion/expiry proof, old-image ingress/body denial, browser/authenticated checks, service restart impact, raw-off release stage, three-boolean accepted settings, monitoring duration and rollback/disable triggers. **Stop for explicit operator approval of this sheet** before any live mutation. The older release record and any config-hash drift retain their own acceptance fence.

## C. Separately approved live sequence (not authorized by this planning task)

- [ ] Fence administrator and account/config writes; recheck image/config/metadata/Compose hashes and private backup. Abort on drift. Stage committed `main` candidate and switch via exact Compose-built image and `up -d --no-build` with raw=false. Check running/healthy, exact ID, zero restarts/OOM, source/mode, predicted config/metadata migration, Legacy account ownership/isolation and local/public/internal plus **authenticated** management projections. Failure before mutation is no-op; after mutation restore prior exact image/config only behind the fenced rollback rules (never run old image on v2 data, never expose old shared-key management).
- [ ] Introduce only the reviewed Switcher memory/ready-flag change while raw remains false; verify `rawBodyAvailable`, health and unchanged routing/account semantics. Do not interpret flag availability as raw acceptance. On failure restore the exact previous infrastructure without modifying production raw settings.
- [ ] With independently authenticated Cookie+CSRF, persist all three accepted booleans in one `POST /api/logs/settings` (expected full=true, error=true, raw=true only after a fresh read and cutover approval); immediately re-read and confirm. Lost response is unknown: read before retry. Do not edit `config.json` directly or claim a protected 401 is an admin read. Verify profile `raw-full` and safe Header projection using authorized bounded synthetic checks, not by extracting a production body. Monitor traffic health, drops, memory and 48h maintenance for the agreed window.
- [ ] On regression, first persist/read back raw=false with previously accepted detail settings **before** removing ready flag or lowering memory; otherwise an unready persisted raw=true suspends all detail capture. If authentication is unavailable, isolate raw ingress/capture and report blocked recovery rather than guessing. Preserve private raw groups under their TTL/backup contract and preserve operator saves. Separate old/new release acceptance evidence from raw-mode acceptance; archive only after every gate passes, else leave the task blocked and raw off.

Rollback points: before any live mutation prove a no-op; after raw-off image/infra switch restore exact previous compatible state under a hash fence; after raw-on disable/read back raw first; to start old shared-key code, first isolate management/raw child and restore a complete compatible v1 config/metadata snapshot only when no operator write has intervened.
