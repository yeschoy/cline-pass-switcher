# Parent integration plan (planning draft)

- [x] Review and approve each child's PRD/design/implement independently; curate both `implement.jsonl` and `check.jsonl` with real spec/research context before each child starts. Do not start this coordination-only parent as an implementation task.
- [x] Complete independent admin login first; local tests covered client-key management denial, legacy localStorage removal, one-time bootstrap, sessions/change-password/logout and rollback. The live migration checked client-key denial and first change; rollback was rehearsed with a synthetic account, not executed on production. Post-restart page access and the later config-save intent were confirmed by the operator, not independently tested with an admin session; the deployment record was finalized against the preserved current config. See `../archive/2026-09/09-24-separate-console-login/research/deployment-handoff-20260924.md`.
- [x] Complete monthly reference quota display and multi-window forecast; then quota exhaustion policy using the existing quota job, with full 3-window concurrency/restart/reset/error matrix.
- [x] Complete model/provider page and reference-price estimator with coverage and missing-price cases; browser VM/static, then real-browser navigation, keyboard and narrow-width checks.
- [x] Complete local raw-body capture only after auth acceptance. First test isolated local mock DATA_DIR and saved sanitized legacy groups. Then test 35 MiB/512 MiB admission, 48h expiry, access denial, old/new mode transitions and model traffic parity.
- [x] For every child run the narrowest relevant tests first, scope-wide tests, Node checks and full `npm test` with credential/base env overrides removed; run `git diff --check`. Re-read spec contracts before editing and update changed executable contracts under `.trellis/spec/` in English.
- [x] Complete local parent integration review: account quota projection vs admission, provider attribution vs request finalizer, authentication vs all admin/log URLs, raw-body expiry vs storage maintenance, and startup/rollback compatibility. Evidence and limitations are in `integration-report.md`; no production acceptance is inferred.
- [ ] **Release gate (separately authorized):** target-container load and authenticated browser acceptance for the raw mode, 48h private backup exclusion/expiry, old-image rollback isolation, copied-data schema/hash migration and live post-switch checks. Keep raw off until every relevant gate passes; preserve unrelated working-tree/operator files.
