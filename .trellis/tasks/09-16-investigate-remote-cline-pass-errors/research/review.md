# 当前生产错误分析独立复核

## 结论

复核时间：2026-09-15 21:12:42 UTC。

在修正两处证据表述后，`current-production-analysis.md` 满足本任务 PRD；未发现剩余的阻断性统计错误、授权越界或敏感内容泄漏。核心数量可按原报告的 2026-09-15 20:59:18 UTC 快照截止时间独立重算，当前容器、最近 24 小时、历史缺口和 NewAPI 短时间窗的结果均与报告一致。

## 已发现并修正

1. **NewAPI 因果措辞偏强**
   - NewAPI 是 Switcher 的下游观察证据，34 个当前错误请求 ID 在 NewAPI 日志中直接匹配为 0，且 NewAPI 同期承载其他流量。
   - 原文个别“NewAPI 通道根因高概率”“来自对应渠道”的说法超过证据能力。
   - 已改为：NewAPI 只证明相同时间窗内下游也观察到 500；不能据此确认 NewAPI、具体渠道、账号或供应商是起因。

2. **后续无失败窗口的秒级边界不明确**
   - 严格截止 `21:01:45.000 UTC` 时是 288 个请求（285 成功、3 取消）。
   - 覆盖完整的 `21:01:45` 秒、截止 `21:01:45.999 UTC` 时才是报告所述 289 个请求（286 成功、3 取消）。
   - 已把报告改为精确毫秒边界，数量本身无需变更。

## 独立验证证据

### 1. 1,187 条错误、985 个 requestId 与 885–985 范围

按原快照截止时间只读重算：

| 区间 | 错误尝试 | 唯一 requestId | 当前请求日志可关联且最终失败 |
|---|---:|---:|---:|
| 2026-09-13 12:42:44–09-14 06:16:59 UTC | 801 | 603 | 当前文件不可关联；与 09-14 历史只读报告的 603 个最终失败一致 |
| 2026-09-14 06:17:00–13:44:10 UTC | 104 | 100 | 0；请求记录已不保留 |
| 2026-09-14 13:44:10–快照 | 282 | 282 | 282 |
| 合计 | 1,187 | 985 | 已确认 603 + 282 = 885；缺口另有 0–100 个可能最终失败 |

因此范围为 `885–985`，不是 1,187。错误 identity 重复数为 0，请求 requestId 重复数为 0；差异来自失败尝试与最终请求的计数单位、重试，以及两类日志保留起点不同。

### 2. 当前容器与最近 24 小时

- 当前容器窗口：363 请求 = 326 成功 + 34 最终失败 + 3 客户端取消。
- 状态：326×200、28×500、5×502、1×429、3×499。
- 当前错误日志：34 条、34 个 requestId；其 requestId 集合与 34 个最终失败请求完全相等。
- 最近 24 小时：1,341 请求 = 1,268 成功 + 67 最终失败 + 6 客户端取消；状态分布与报告一致。
- 全部重叠请求窗口：1,639 请求 = 1,351 成功 + 282 最终失败 + 6 客户端取消。

### 3. 当前 34 个失败的两簇与旧误报签名

- GLM：28 个非流式失败，全部 500；14:41 有 11 个，14:42 有 17 个；安全分类均为空上游响应。
- DeepSeek：6 个流式失败；5×502、1×429；对应错误尝试的 upstream HTTP 状态均为 200；16:14、16:16、16:17、16:19、16:20、16:23 各 1 个。
- 当前错误 requestId 与当前最终失败 requestId 一一对应，没有尝试数放大。
- 旧误报签名的独立判定条件为：`result=failed`、`stream=true`、最终状态 502、且所有投影 attempt 状态为 200。该签名在当前容器、最近 24 小时和全部当前保留请求窗口均为 0。
- 最后失败为 16:23:01.598 UTC；至 21:01:45.999 UTC 的后续 289 个请求中 286 成功、3 个客户端取消、0 个最终失败。

### 4. NewAPI 相关性

只读聚合复现：

- 34 个当前 Switcher 错误 requestId 在 NewAPI 日志中的直接文本匹配数：0。
- 14:40–14:44 UTC：30 条 channel error 500，其中 28 条不带额外 `failed` 标记。
- 16:13–16:25 UTC：9 条 channel error 500。

这些数字只能支持时间一致性，不能建立请求级关联或因果。修正后的报告已明确这一限制。

### 5. 不变性与秘密检查

独立抽查前后以下值一致：

- Switcher container ID：`445a30f319d25d313062ca2b72c549fa3bac31cc4c3b99ae0364769ad5677144`
- image ID：`sha256:62963e5048f4ccdf83b8cdf88de28e517b39e44fdd53ea6b7b0d1a704e697ae0`
- startedAt：`2026-09-15T11:45:27.389044065Z`
- restartCount：0
- OOMKilled：false
- health：healthy
- `config.json` SHA-256：`87d5ae2df08e1774ff76f35f186f27c12c8eb78469f415df3cf40403341d6089`

抽查仅使用只读 SSH、`docker inspect/logs`、哈希读取及远程内存聚合；未写远程文件、未调用写接口、未重启、未部署、未清理日志。报告敏感模式扫描未发现 Bearer token、Authorization/Cookie 值、proxyKey 值、`sk-` 类密钥或含凭据 URL；正文只保留允许的聚合、伪名、状态及代表 requestId。

### 6. 授权边界

报告建议均为后续处理或验证建议，没有把重启、清理、部署、恢复或代码变更描述为本次已执行动作。端到端 requestId 透传被明确标为后续单独变更任务，未超出本次只读授权。

## 残余风险

- 09-14 06:17–13:44 UTC 的 100 个 requestId 缺少最终请求记录，无法在现有只读数据上把 885–985 进一步收窄。
- NewAPI 没有可关联的 Switcher requestId，具体渠道、账号或供应商根因仍未确认。
- 报告中的 NewAPI 近 24 小时总行数会随精确采集时刻变化，原报告未记录该次采集的毫秒级起止点；短时间窗的 30/9 条关键计数已独立复现，不影响根因等级。
- 本次复核重放原快照截止时间；生产日志在服务继续运行时会自然追加，不能把后续总量与原快照直接比较。

## 本次修改文件

- `.trellis/tasks/09-16-investigate-remote-cline-pass-errors/research/current-production-analysis.md`
- `.trellis/tasks/09-16-investigate-remote-cline-pass-errors/research/review.md`
