# Whole-flow execution plan (after planning review/start)

- [x] Load backend/frontend scenario specs and parent read-only audits/two-run smoke. Evidence and limitations recorded in `research/results.md`.
- [ ] Build reproducible isolated harness/profile fixtures for **all** flows. `research/profile.mjs` now covers local startup, small/512 KiB/49 MiB chat, three real attempts, 1/10 accounts and concurrent waiters, SSE/paused consumer/cancel, sparse/1200-bucket metadata, ordinary/error/full detail modes, manual quota refresh, and common management reads. Local logs (33k ordinary rows/2k detail roots), accelerated eight-account quota scheduler, serial direct/CONNECT/SOCKS reuse and sparse browser smoke are now measured in `research/remaining-flows.md`/`browser-results.md`. Still pending: near-cap/slow-disk recovery, TLS/concurrent proxy pressure, production-cadence quota contention with large metadata, dense browser redraw and sustained/burst throughput.
- [x] Attribute 1200-bucket chat/GET slowdown with measured prune, sync stringify/write/rename and GET projection sections before editing; rank and defer in `research/results.md`.
- [x] Change only metadata encoding in existing atomic write owner, preserve per-terminal durability; add known-zero/restart/legacy-format regression test and use existing rename-fault suite.
- [ ] Re-run identical sparse/dense harness (done) and finish remaining whole-flow traces (pending); actual Chromium/390px sparse console login/filter/navigation smoke done via installed Playwright fallback, but high-cardinality browser and proxy/log saturation remain pending. Current A/B numbers, CPU/loop/RSS/heap and constraints in `research/results.md`.
- [ ] Focused integration 109/109, env-scrubbed full test 294/294, syntax/inline VM/diff checks passed; spec updated. Independent review and commit (including Trellis artifacts) remain with main agent; later children must repeat relevant cases.

Rollback: preserve prior metadata and tests; each measured optimization is a separate reversible change. Production deployment/performance validation is not authorized here.
