# Independent administrator deployment — handoff (2026-09-24)

## Live state (verified, not final acceptance)

- User corrected the public origin to `https://clinepass.yeschoy.io`. The committed/pushed source `main == origin/main` at release creation was `c35cd746b582bf8b862e9577fae7bf4b04f99a70`. Release `20260924-052044-c35cd746-admin-auth`, candidate image `sha256:2747552c4fed2b9cbee9461682a6b5cf50fba47b77a209f55c7e028982047571` is running on the canonical production host.
- The existing Cloudflare Tunnel directly targeted host `127.0.0.1:3123`. The one-time admin migration moved only the application's host-published port to loopback `3124`, leaving its container/internal-network port `3123`, and inserted a host-loopback Nginx gateway at tunnel port `3123`. It replaces both HTTPS attestation Headers using a private proxy token; the public HTTPS endpoint, local gateway and `ai-internal` alias passed. Nginx syntax and the gateway's spoofed-Header replacement/unknown-Host checks passed against a synthetic local account, with no paid upstream request.
- The allowlisted archive SHA-256 was `6f4565ee23e60976bcc7996c925826ac35db209cef843dc6e4b0148f5836cc1a`. Five built `/app` source hashes matched the archive; UID/GID `1000:1000` could read them. The exact Compose-built candidate started under production hardening against a private copied `DATA_DIR` without network access. The forced initial change, old-client-key denial, protected statistics/log settings, bootstrap replay denial and forged direct proxy denial passed in that isolated copy.
- User retrieved the one-time code via their own SSH session. The live switch passed image/health/zero-restart, public meta, pending admin state, client-key denial, internal alias, account count 10/sticky mode and unchanged `config.json` hash. The user reported successfully performing the mandatory first password change and login; the backend independently confirmed `admin-auth.json` initialized with owner-only mode, public auth state initialized and old client key denied. No administrator password, initial code, proxy token, Cookie, CSRF value or operator JSON contents were printed or copied into this report.
- The initialized state was backed up privately. An isolated no-bootstrap restart with the exact image preserved both config and administrator verifier bytes. The live one-time initialization code and bootstrap flag were then removed from the private environment file and the same image force-recreated; the old stopped rehearsal container with the code in Docker metadata was removed. Public meta/auth state, image/health/restart 0 and config hash passed immediately afterward. A later read-only check at container age >2,000 seconds still found healthy/restart 0, public meta 200, initialized admin state and **no initialization code in the running container environment**.
- Nine management routes (`accounts`, `models`, `statistics`, request/error/detail log projections, security and config) rejected the old client key; public `/api/meta` remained 200, client-key-free model endpoint returned 401, and internal alias returned 200. No live/paid model completion, New API SSE end-to-end or independent authenticated management-script check was performed.

## Unresolved final gates / concurrent operator change

- The user has **not yet confirmed** that, after the no-bootstrap container restart, their new-password browser session can read account administration, statistics and detailed-log views. Their earlier successful browser login was before that restart. Obtain a non-secret confirmation before claiming complete management UI acceptance; do not request their password or session token.
- Root-private `/opt/cline-pass-switcher/deployment.json` remains labelled `awaiting-admin-first-change` although the administrator verifier is now initialized. Finalize it only after post-restart acceptance, recording the actually current safe config hash and any later operator change rather than claiming that the original hash remains current.
- The deployment baseline/config hash was `9c909277a5746377f9f40418098eb0de85b0915967c1bc88ded5ffc3073499a0`, verified unchanged immediately after both live switch and no-bootstrap restart. A later **read-only** inspection found `config.json` modified at `2026-09-24T06:29:45Z` to hash `ba6f66bcfe4e1bf3c250937e8849768b736fe3bbe97cb9ee5c273a2319fe93b5`. The only top-level changes were `detailedLogging` (true → false), `errorDetailLogging` (false → true), `publicBaseUrl` (old `.com` → canonical `.io`) and one existing global `perModel` route; account count/fields and client key did not change. This occurred **after** the deployment gate and may be an intentional administrator write. Preserve it; do not restore the older backup, revert a route, or label it a deployment normalization without operator confirmation. If unexpected, investigate the management write path separately before final acceptance.
- The old exact image, private initial/final configuration/metadata/Compose backups, post-first-change administrator-state backup, candidate and admin-isolating rollback gateway configurations and built image were retained. The old image behind the denying rollback gateway was rehearsed with a synthetic account: public meta 200, old client key and detailed-management URL 403, model no-key 401. No production rollback was required. Raw/unredacted detailed-body mode was not implemented or enabled.
- Local task-created staging paths and remote rehearsal copies still exist. The independent one-time code is scrubbed from the running environment and private env file; do not casually print or delete operator backups. Ask for consent before clearing non-business intermediates after the task completes, retaining Trellis logs.

## New-session continuation

1. First reconcile the post-restart browser read gate and the 06:29 UTC operator config change without exposing credentials. Repeat healthy/current image/zero restarts, initialized auth state, client-key-denial, public/internal meta and account-count checks. Do not treat the later operator save as a deployment-caused config mismatch that should be overwritten.
2. Once accepted, atomically update the root-private deployment record with a truthful completed status/current config hash and safe evidence; write the check report, update this task's implementation checklist, run local documentation validation, commit Trellis changes and push `main`. Keep a tested rollback path.
3. The parent `.trellis/tasks/09-24-diagnostics-quota-console-plan` and other four children remain planning-only. The user's requested monthly account-remaining and immediately-usable **estimated** quota belongs to `09-24-monthly-quota-statistics`; the model/provider success/cache/Token/reference-price page belongs to `09-24-model-provider-statistics`. Do not call either feature deployed merely because the older statistics page still shows per-account 5h/week/month percentages and health.
